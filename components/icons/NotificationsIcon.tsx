import { Bell } from "lucide-react";
import { IconBase } from "./IconBase";

export function NotificationsIcon({ className }: { className?: string }) {
  return <IconBase icon={Bell} className={className} />;
}
