import { MessageSquareMore } from "lucide-react";
import { IconBase } from "./IconBase";

export function MessagesIcon({ className }: { className?: string }) {
  return <IconBase icon={MessageSquareMore} className={className} />;
}
