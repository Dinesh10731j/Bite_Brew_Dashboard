import { Settings } from "lucide-react";
import { IconBase } from "./IconBase";

export function SettingsIcon({ className }: { className?: string }) {
  return <IconBase icon={Settings} className={className} />;
}
