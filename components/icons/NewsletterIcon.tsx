import { Mail } from "lucide-react";
import { IconBase } from "./IconBase";

export function NewsletterIcon({ className }: { className?: string }) {
  return <IconBase icon={Mail} className={className} />;
}
