import { Home } from "lucide-react";
import { IconBase } from "./IconBase";

export function DashboardIcon({ className }: { className?: string }) {
  return <IconBase icon={Home} className={className} />;
}
