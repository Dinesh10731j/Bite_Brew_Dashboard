import { Empty } from "@/components/shared/ui/Empty";

export function OrderTimeline() {
  return <Empty title="No Static Timeline" description="Order timeline is loaded from backend order detail data." />;
}