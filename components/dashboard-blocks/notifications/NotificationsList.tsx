import { Empty } from "@/components/shared/ui/Empty";

export function NotificationsList() {
  return <Empty title="Notifications Moved" description="Use Notifications workspace for live backend data." />;
}